#include<stdio.h>
#include<ctype.h>

int main(int argc,char** argv){
	
	char ch;
	char str[2];
	while(true){
		str[0] = getchar();
		if(!isdigit(str[0]))
			goto readLine;
		str[1]= getchar();
readline:
		while(st)
	}

	return 0;
}
