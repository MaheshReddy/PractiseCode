#include<iostream>
#include"CommonLib.h"

template <class myType> // myType should be iterable object else it will fail
vector<myType> permute(myType origVar)
{
	list<myType>::iterator it = origVar.begin();
	while()
}
